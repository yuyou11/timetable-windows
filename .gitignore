# ============================================================
#  .gitignore —— 告诉 git「哪些文件不要进版本库」
# ============================================================
#
# 判断标准只有一条：**这个文件是「人写出来的」，还是「机器生成出来的」？**
# 人写出来的（.py / .js / .html / .md）要进版本库，因为丢了就找不回来；
# 机器生成出来的（打包产物、日志、缓存）不该进，因为只要有源码随时能再生成一遍。
#
# 把生成物提交进去有三个坏处：
#   1. 仓库体积暴涨（这个项目里 dist/ + build/ 一个就有 145 MB，而源码不到 2 MB）
#   2. 每次打包都产生几百个文件的「改动」，真正的代码改动被淹没了
#   3. **二进制会永久留在历史里** —— 后来删掉文件也删不掉历史，仓库只会越来越大
#
# 规矩：**打包产物不进版本库**，这一条在几乎所有语言里都成立。
# （手机版仓库的 .gitignore 里写的是 build/ 和 dist/，网页项目里对应 node_modules/。）

# ------------------------------------------------------------
#  打包 / 编译产物
# ------------------------------------------------------------
# 前导斜杠表示「只匹配仓库根目录下的」，不写的话会匹配到任意层级的同名目录
/dist/
/build/
*.exe

# PyInstaller 的 .spec 是 build.py 每次打包自动生成的中间文件，
# 里面写的是**本机绝对路径**（打包时那条命令的工作目录），
# 提交上去对别人没用，还会暴露本机的目录结构。
*.spec

# ------------------------------------------------------------
#  运行 / 调试痕迹
# ------------------------------------------------------------
/build-logs/

# Python 的字节码缓存
__pycache__/
*.py[cod]

# ------------------------------------------------------------
#  数据文件 —— ⚠️ 这一条最重要
# ------------------------------------------------------------
# 导出的课表、备份、以及任何从程序导出的 JSON。
#
# 里面是**个人课表**（学校、年级专业、课程名、教室号）。
# 这个仓库是公开的，一旦提交进去，删掉文件也删不掉历史。
#
# 例外：tools/sample-jiaowu.json 是**通用样例**（示例大学），
# 它需要进版本库，所以下面用 ! 单独放行。
课表*.json
*课表*.json
!tools/sample-jiaowu.json
日型-全开.json

# ------------------------------------------------------------
#  编辑器 / 操作系统
# ------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
