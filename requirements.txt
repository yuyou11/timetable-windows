# 运行时依赖 —— python -m app.main 和打包后的 exe 都需要
#
# 之前这里什么都没有，README 只写了「标准库 + pywebview」，其实还有两个：
#   pystray  app/tray.py 的系统托盘（缺了不影响主功能，只是没有兜底入口）
#   Pillow   app/tray.py 画托盘图标（同上，缺了会退回纯色块）
# 版本号是开发机上实测可用的版本，写的是下限而不是钉死 —— 免得每次升级都要改这里。

pywebview>=6.2
pystray>=0.19
Pillow>=12.0

# ------------------------------------------------------------
# 只有打包 / 开发才需要，日常运行不用装
# ------------------------------------------------------------
# pyinstaller  build.py 打包成 exe
# Pillow       tools/gen_icon.py 生成图标时也要用（同上，已列在运行时）

pyinstaller>=6.22
